# Dev: để trống — fetch tới /api/... qua Vite proxy (vite.config.ts → 127.0.0.1:8010), không cross-origin.
# Nếu gọi thẳng API khác port: ví dụ http://127.0.0.1:8010 (sẽ cần CORS trên backend).
VITE_API_BASE_URL=